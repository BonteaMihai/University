Write a C program which receives a filename as a command line parameter.
The parent writes the contents of the file to the child process, which turns it to uppercase and
writes it back to the parent, which prints it to the screen.