Write a C program which receives a number as command line parameter.
The program creates a child. The number is sent back and forth between the parent and the child,
each subtracting a random number less than 100. The program stops when the number given reaches 0 or below.