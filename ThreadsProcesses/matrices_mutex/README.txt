Write a C program which computes the sum of two square matrices, using a thread for every line.
The threads will choose the line based on a global variable, current_line, which will be incremented
before processing that line.